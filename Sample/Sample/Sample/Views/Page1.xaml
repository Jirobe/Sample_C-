<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sample.Views.Page1">
    <ContentPage.Content>
        <StackLayout>
            <Label Text=" "/>
            <Button  Text="戻る" Command="{Binding BackCommand}"/>
            <Button  Text="メッセージダイアログ" Command="{Binding MessageCommand}"/>
            <Entry Placeholder="なにか入力してください" Text="{Binding MessageText}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>